package DAO;

public class UserDAO {
    // You will have something like getUser() that gets one user if you give it a user name
    // so it's reading from user id column doing a select * from user etc.
}
